package com.elevator.model;

/**
 * Represents the direction an elevator is currently travelling,
 * or IDLE when stationary with no pending requests.
 */
public enum Direction {
    UP,
    DOWN,
    IDLE
}
